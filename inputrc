# show misc
set mark-directories on
set mark-symlinked-directories on
set show-all-if-unmodified on
set show-all-if-ambiguous on
set visible-stats on
set colored-stats on

# treat underscores and hyphens as the same
set completion-map-case on

# case insensitve auto completion
set completion-ignore-case On

# ask before showing >200 results
set completion-query-items 200

# show all completions
set page-completions off

# more intelligent auto-complete behaviour for partial words
set skip-completed-text on

# allow UTF-8 input and output, instead of showing stuff like $'\0123\0456'
set input-meta on
set output-meta on
set convert-meta off

# enable vim style commands on shell
set editing-mode vi
set show-mode-in-prompt on

# cursor changes like vim when insert or command mode
set vi-ins-mode-string \1\e[6 q\2
set vi-cmd-mode-string \1\e[2 q\2

# arrow keys
"\e[A": history-search-backward
"\e[B": history-search-forward

# ctrl keys
"\e[1;5C": forward-word
"\e[1;5D": backward-word

# backwards kill word
"": backward-kill-word
